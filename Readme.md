# 🌤 WeatherBot
A simple Telegram bot that displays the minimum and maximum daily temperatures for 30 provinces in Iran.

---

## 🌟 Features
- Shows minimum and maximum temperature for 30 provinces  
- Easy-to-use inline keyboard  
- Fast and reliable

---

## ⚙️ Install and Run

1️⃣ **Install dependencies:**
```bash
pip install -r requirements.txt
```

2️⃣ **Set up your bot token:**  
Insert your *Telegram Bot Token* (from BotFather) into the `TOKEN` variable in the `bot.py` file:
```python
TOKEN = "YOUR_TELEGRAM_BOT_TOKEN_HERE"
```

---

## 🇮🇷 توضیحات فارسی

این ربات تلگرام برای نمایش حداقل و حداکثر دمای ۳۰ استان ایران طراحی شده است.  

### 💡 امکانات:
- نمایش دمای حداقل و حداکثر روزانه برای ۳۰ استان  
- رابط کاربری ساده با دکمه‌های درون‌خطی (Inline Keyboard)  
- سرعت بالا و پاسخ‌گویی سریع  

---

## ⚙️ نصب و اجرا

۱️⃣ **نصب کتابخانه‌ها:**
```bash
pip install -r requirements.txt
```

۲️⃣ **تنظیم توکن ربات:**  
*توکن ربات تلگرام* خود (از BotFather) را در متغیر `TOKEN` در فایل `bot.py` وارد کنید:
```python
TOKEN = "YOUR_TELEGRAM_BOT_TOKEN_HERE"
```

---

## 🗂 ساختار فایل‌ها  

```
WeatherBot/
│
├── bot.py              # فایل اصلی ربات
├── wetter.py           # تابع دریافت اطلاعات دما
├── requirements.txt    # کتابخانه‌های مورد نیاز
└── README.md           # توضیحات پروژه
```
---



